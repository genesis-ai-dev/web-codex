name: Backend CI/CD Pipeline

on:
  push:
    branches: [ main ]
    paths:
    - 'backend/**'
    - '.github/workflows/backend.yaml'
  pull_request:
    branches: [ main ]
    paths:
    - 'backend/**'
    - '.github/workflows/backend.yaml'


defaults:
  run:
    working-directory: ./backend

env:
  NODE_VERSION: '22'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/backend

jobs:
  # Code quality and testing
  test:
    runs-on: ubuntu-latest
    
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
      
      dynamodb:
        image: amazon/dynamodb-local:latest
        ports:
          - 8000:8000
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        # cache: 'npm'
        # cache-dependency-path: ./backend/package-lock.json
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linting
      run: npm run lint
    
    - name: Run type checking
      run: npx tsc --noEmit
    
    - name: Run tests
      run: npm run test:coverage
      env:
        NODE_ENV: test
        REDIS_URL: redis://localhost:6379
        DYNAMODB_ENDPOINT: http://localhost:8000
    
    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage/lcov.info
        flags: unittests
        name: codecov-umbrella
    
    - name: Build application
      run: npm run build

  # Security scanning
  security:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run security audit
      run: npm audit --audit-level=moderate
    
    - name: Run dependency check
      run: npx audit-ci --moderate

  # Docker build and push
  docker:
    runs-on: ubuntu-latest
    needs: [test, security]
    if: github.event_name == 'push'
    
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Login to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  # Deploy to development
#   deploy-dev:
#     runs-on: ubuntu-latest
#     needs: [docker]
#     if: github.ref == 'refs/heads/develop'
#     environment: development
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: us-west-2
    
#     - name: Update kubeconfig
#       run: |
#         aws eks update-kubeconfig --region us-west-2 --name vscode-platform-dev
    
#     - name: Deploy to Kubernetes
#       run: |
#         # Update image tag in deployment
#         kubectl set image deployment/backend-api \
#           backend-api=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:develop \
#           -n vscode-platform-dev
        
#         # Wait for rollout to complete
#         kubectl rollout status deployment/backend-api -n vscode-platform-dev

#   # Deploy to production
#   deploy-prod:
#     runs-on: ubuntu-latest
#     needs: [docker]
#     if: github.ref == 'refs/heads/main'
#     environment: production
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: us-west-2
    
#     - name: Update kubeconfig
#       run: |
#         aws eks update-kubeconfig --region us-west-2 --name vscode-platform-prod
    
#     - name: Deploy to Kubernetes
#       run: |
#         # Update image tag in deployment
#         kubectl set image deployment/backend-api \
#           backend-api=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
#           -n vscode-platform
        
#         # Wait for rollout to complete
#         kubectl rollout status deployment/backend-api -n vscode-platform
    
#     - name: Verify deployment
#       run: |
#         # Check if all pods are ready
#         kubectl wait --for=condition=ready pod -l app=backend-api -n vscode-platform --timeout=300s
        
#         # Run health check
#         kubectl port-forward svc/backend-api 8080:80 -n vscode-platform &
#         sleep 5
#         curl -f http://localhost:8080/api/health || exit 1

#   # Notification
#   notify:
#     runs-on: ubuntu-latest
#     needs: [deploy-prod]
#     if: always()
    
#     steps:
#     - name: Notify deployment status
#       uses: 8398a7/action-slack@v3
#       with:
#         status: ${{ job.status }}
#         channel: '#deployments'
#         webhook_url: ${{ secrets.SLACK_WEBHOOK }}
#       env:
#         SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
